<div class='container'>
  <h2>商品詳細</h2>
  <div class='row justify-content-center'>
    <div class='col-3 d-flex justify-content-center'>
     <%= image_tag @item.image, size:"300x300" %>
     </div>
     <div class="col-1"></div>
     <div class="col-8 d-flex justify-content-center">
       <div class="row">
      <dt class="col-3">商品名</dt>
      <dd class="col-9"><%= @item.name%></dd>
      <dt class="col-3">商品説明</dt>
      <dd class="col-9"><%= @item.introduction%></dd>
      <dt class="col-3">ジャンル</dt>
      <dd class="col-9"><%= @item.genre.name%></dd>
      <dt class="col-3">税込み価格&emsp;&emsp;&emsp;&emsp;(税抜き価格)</dt>
      <dd class="col-9"><%= (@item.price*1.1).floor %>(<%= @item.price %>)円</dd>
      <dt class="col-3">販売ステータス</dt>
        <%if @item.sales_status == true %>
        <dd class="col-9 text-success">販売中</dd>
        <% else %>
        <dd class="col-9 text-secondary">販売停止中</dd>
        <% end %>
      </dd>
      </div>
    </div>
  </div>
  <div class="d-flex justify-content-center">
            <%= link_to '編集する', edit_admin_item_path(@item), class: "btn btn-sm btn-success edit_book_#{@item.id}" %>
          </div>
</div>