<div class='container'>
  <div class='row'>
    <div class='col-md-8 offset-md-1'>
      <h2>商品詳細</h2>
      <table class='table'>
        <td>
          <div>
            <%= image_tag @item.image, size:"200x200" %>
          </div>
        </td>
        <td>
          <div>
            商品名: <%= @item.name %>
          </div>
          <div>
            商品説明: <%= @item.introduction %>
          </div>
          <div>
            ジャンル: <%= @item.genre.name %>
          </div>
          <div>
            税込み価格＜ｂｒ＞（税抜き価格）：
            <%= @item.price*1.1 %>(<%= @item.price %>)円
          </div>
          <div>
            販売ステータス：
            <%if @item.sales_status == true %>
              販売中
            <% elsif @item.sales_status == false %>
              販売停止中
            <% end %>
          </div>
          <div>
            <%= link_to '編集する', edit_admin_item_path(@item), class: "btn btn-sm btn-success edit_book_#{@item.id}" %>
          </div>
        </td>
      </table>
    </div>
  </div>
</div>