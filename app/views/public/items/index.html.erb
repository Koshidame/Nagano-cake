<div class="container px-5 px-sm-0">
  <div class="row">
     <div class="col-md-8 offset-md-1">
     <h2>商品一覧</h2>
      
      <% @items.each do |item| %>
      <div>
      <%= image_tag item.image, size:"50x50" %>
      </div>
      <div>
        <%= item.name %>
      </div>
      <div>
       ¥<%= item.price %>
      </div>
      <% end %>
      </table>
    </div>
  </div>
</div>